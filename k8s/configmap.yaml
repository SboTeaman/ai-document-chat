apiVersion: v1
kind: ConfigMap
metadata:
  name: kb-config
  namespace: knowledge-base
data:
  # Non-secret runtime configuration. Secrets live in `kb-secrets`.
  DJANGO_SETTINGS_MODULE: "config.settings.production"
  DEBUG: "False"
  ALLOWED_HOSTS: "kb.example.com"
  ALLOWED_ORIGINS: "https://kb.example.com"
  CSRF_TRUSTED_ORIGINS: "https://kb.example.com"
  # Ingress terminates TLS, so trust its forwarded-proto header.
  TRUSTED_PROXY_IPS: ""

  OLLAMA_BASE_URL: "http://ollama:11434/v1"
  OLLAMA_EMBEDDING_MODEL: "nomic-embed-text"
  OLLAMA_CHAT_MODEL: "llama3.2:3b"
  OLLAMA_MAX_RETRIES: "3"

  CHUNK_SIZE_TOKENS: "512"
  CHUNK_OVERLAP_TOKENS: "64"
  EMBEDDING_BATCH_SIZE: "100"
  EMBEDDING_CHECKPOINT_TTL: "3600"
  MAX_UPLOAD_SIZE_MB: "20"
  MAX_CHUNKS_PER_DOCUMENT: "5000"
  WORKSPACE_STORAGE_QUOTA_MB: "2048"

  SEARCH_VECTOR_WEIGHT: "0.7"
  SEARCH_FTS_WEIGHT: "0.3"
  SEARCH_MIN_SCORE: "0.15"
  SEARCH_FTS_LANGUAGE: "english"
  QUERY_EMBEDDING_CACHE_TTL: "300"

  JWT_ALGORITHM: "HS256"
  ACCESS_TOKEN_LIFETIME_MINUTES: "15"
  REFRESH_TOKEN_LIFETIME_DAYS: "7"
  RATELIMIT_ENABLE: "True"

  AWS_S3_REGION_NAME: "us-east-1"
  AWS_S3_BUCKET_NAME: "knowledge-base-files"
